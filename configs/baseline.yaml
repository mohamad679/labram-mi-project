# Phase 1 baseline config — fill in as Phase 1 progresses
subjects: [1, 2, 3, 4, 5]   # start small, expand once pipeline is verified
n_outputs: 2
batch_size: 16
learning_rate: 0.0001
epochs: 20
freeze_backbone: true
checkpoint_every_n_steps: 50
