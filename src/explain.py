"""
Phase 3 — Explainability via attention rollout.

NOT implemented yet — stub for Phase 3.
Scope reminder: attention rollout only. Do NOT implement SHAP —
flagged explicitly as a scope-creep risk in the roadmap.
"""


def attention_rollout(model, x):
    """TODO (Phase 3): extract attention weights from LaBraM's
    transformer layers and produce a rollout visualization for a
    handful of representative samples (3-4 is enough)."""
    raise NotImplementedError
